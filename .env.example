# Your Neon PostgreSQL connection string. Get this from your Neon project
# dashboard — never commit a real value here or anywhere in the repo.
DATABASE_URL=postgresql://neondb_owner:npg_4IgOsB5oTGEA@ep-royal-dew-axfcrfa0-pooler.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Required. A long, random, secret string used to sign session cookies —
# there is no built-in fallback, the app will refuse to start without it.
# Generate one locally with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Set this in your .env file for local dev AND in Vercel's Environment
# Variables for production — use a DIFFERENT value in each, and never
# commit the real value to git.
SESSION_SECRET=Rwu+Z047n6Y864d83xVyTT6vAKJY/gv0Ak5S4gir7XQ=

PORT=5500

NODE_ENV=development

# Optional — only needed if you want Forgot Password to actually email the
# OTP code instead of just showing it on screen ("demo mode"). Example
# values for a Gmail account using an App Password:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-address@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM=your-address@gmail.com